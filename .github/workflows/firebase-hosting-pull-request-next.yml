# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy on PR - Next
'on':
  pull_request:
    paths:
      - mt-next/**
      - .github/workflows/firebase-hosting-pull-request-next.yml
jobs:
  build_and_preview-next:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v2
      - name: 'Install'
        run: yarn install --frozen-lockfile
        working-directory: ./mt-next
      - name: 'Build'
        run: yarn build
        working-directory: ./mt-next
      - name: 'Deploy'
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MATAHARITERBITNEXT }}'
          projectId: matahariterbitnext
          entryPoint: ./mt-next
